[pytest]
testpaths = tests
# Добавляем корень backend_project в sys.path, чтобы `import app...` работал
# при запуске `pytest` (а не только `python -m pytest`), в т.ч. в CI.
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
